# OpenAI 兼容网关三件套:拷贝本文件为 local.env 再填(local.env 不入库)。
# 配好后 worker/reviewer 自动走网关,deveco 配置由 prepare 自动生成,别的什么都不用做。
# 不想用文件的话,设同名环境变量也行(环境变量优先)。
OPENAI_API_BASE=http://your-gateway-host/v1
OPENAI_API_KEY=你的密钥
OPENAI_MODEL=auto
