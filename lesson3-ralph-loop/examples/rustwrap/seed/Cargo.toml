[package]
name = "rustwrap"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "unwind"   # main.rs 用 catch_unwind 兜 todo!(),别改成 abort
